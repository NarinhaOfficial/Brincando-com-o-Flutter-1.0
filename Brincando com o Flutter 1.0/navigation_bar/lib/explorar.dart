import 'package:flutter/material.dart';

class P_Explorar extends StatelessWidget {
  const P_Explorar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}